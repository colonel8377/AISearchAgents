# --- Server & API ---
# 2025年最新版本
fastapi
uvicorn[standard]
pydantic
pydantic-settings

# --- LangChain Ecosystem (2025年最新版本) ---
# 显式列出所有必需的包，确保正确安装
langchain>=0.3.0
langchain-core
langchain-community>=0.3.0  # Vector stores (Redis, PGVector, Chroma)
langchain-openai
langchain-google-genai

# --- Shared Utilities ---
# 2025年最新版本
tenacity
httpx
python-dotenv
beautifulsoup4
trafilatura

# --- Vector Stores ---
# 2025年最新版本
redis>=5.2.0
langchain-chroma>=0.1.0
# chromadb>=0.5.0  # 如果使用直接 chromadb
pgvector>=0.3.0
psycopg2-binary

# --- Scientific ---
# 2025年最新版本
scipy
numpy
scikit-learn

# --- File Parsing (for privacy detection) ---
# PDF parsing
PyPDF2
pdfplumber

# Word document parsing
python-docx==1.1.0

# Image OCR parsing
pytesseract==0.3.10  # Legacy OCR (kept for compatibility)
rapidocr-onnxruntime>=1.3.0  # CPU-friendly OCR for P-Guard 3.0
Pillow>=11.3.0
opencv-python==4.8.1.78

# Excel parsing
openpyxl==3.1.2
xlrd==2.0.1

# Text encoding detection
chardet

# --- Privacy Detection (P-Guard 2.0) ---
# Presidio for PII detection and anonymization
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
# spaCy for NLP engine (required by Presidio)
spacy>=3.7.0
# GLiNER for semantic entity recognition
gliner
# Faker for synthetic data generation
faker

# Note: After installing requirements, download spaCy models:
#   python -m spacy download en_core_web_sm
#   python -m spacy download zh_core_web_sm
# Or use larger models for better accuracy:
#   python -m spacy download en_core_web_lg
#   python -m spacy download zh_core_web_lg

# --- Testing ---
# 2025年最新版本
pytest
pytest-asyncio>=0.25.0
python-multipart>=0.0.6
en-core-web-sm==3.8.0
zh-core-web-sm==3.8.0
transformers
pysbd
decorator